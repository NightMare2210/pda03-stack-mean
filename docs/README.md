# Evidencia funcional — Reto 1 y Reto 2

- `postman-collection.json`: colección exportada con las 5 requests del CRUD (Reto 1).
- `screenshots/`: capturas de cada request corriendo contra el backend real, conectado a MongoDB Atlas, incluyendo el caso de validación rechazada por Zod (Reto 2).

Backend requerido corriendo en `http://localhost:3000` (ver `backend/README.md` para levantarlo).
